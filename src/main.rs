use chrono::{DateTime, NaiveDateTime};
use icalendar::{Calendar, Event};
use std::io::Error;

//fn get_calendar_events(cal: &Calendar) -> Result<Vec<Event>, io::Error> {}
fn main() {
    let foo = env!("foo");
    println!("{}", foo);
    println!("Hello, world!");
}
